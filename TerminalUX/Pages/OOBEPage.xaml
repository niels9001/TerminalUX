<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TerminalUX.Pages.OOBEPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:TerminalUX.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TerminalUX.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="GridCardStyle" TargetType="Grid">
            <Style.Setters>
                <Setter Property="Background" Value="{ThemeResource AcrylicInAppFillColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="{StaticResource OverlayCornerRadius}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="Padding" Value="24" />
            </Style.Setters>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:HeroImage
            x:Name="hero"
            Grid.RowSpan="3"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Opacity="0.7" />

        <StackPanel
            x:Name="FirstTitlePanel"
            Margin="0,48,0,48"
            HorizontalAlignment="Center"
            Orientation="Vertical"
            Spacing="4">
            <TextBlock
                CharacterSpacing="48"
                FontSize="48"
                FontWeight="SemiBold"
                Text="Welcome to Terminal."
                TextAlignment="Center" />
            <TextBlock
                HorizontalAlignment="Center"
                FontWeight="SemiBold"
                Text="Windows Terminal brings all your command line shells together. Here’s how it can boost your productivity."
                TextAlignment="Center" />
        </StackPanel>
        <SelectorBar
            x:Name="selector"
            Grid.Row="1"
            HorizontalAlignment="Center"
            SelectionChanged="SelectorBar_SelectionChanged">
            <SelectorBarItem
                Icon="{ui:FontIcon Glyph=&#xEC6C;}"
                IsSelected="True"
                Text="Tabs &amp; panes" />
            <SelectorBarItem Icon="{ui:FontIcon Glyph=&#xE748;}" Text="Profiles" />
            <SelectorBarItem Icon="{ui:FontIcon Glyph=&#xEDA7;}" Text="Keybinding actions" />
            <SelectorBarItem Icon="{ui:FontIcon Glyph=&#xE8BD;}" Text="Sendinput actions" />
            <SelectorBarItem Icon="{ui:FontIcon Glyph=&#xF4A5;}" Text="Rendering" />
        </SelectorBar>
        <FlipView
            x:Name="flipper"
            Grid.Row="2"
            Margin="0,64,0,0"
            Background="Transparent">
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    ColumnSpacing="24"
                    Style="{StaticResource GridCardStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="560" />
                        <ColumnDefinition Width="*" MaxWidth="480" />
                    </Grid.ColumnDefinitions>
                    <controls:PictureHolder
                        Width="460"
                        Height="286"
                        Margin="-24"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        ImageUrl="ms-appx:///Assets/Picture1.gif" />
                    <TextBlock Grid.Column="1" TextWrapping="Wrap">
                        <Run
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="Tabs &amp; panes" /><LineBreak /><Run Text="You can open new tabs with the “+” button and even tear them out to create new Windows too. Right clicking the tab will give you management options (like tab color and tab name)" />
                        <LineBreak />
                        <LineBreak />
                        <Run Text="Panes let you run multiple command-line applications next to each other within the same tab. This minimizes the need to tab switch and lets you see multiple prompts. " />
                        <LineBreak />
                        <LineBreak />
                        <Run Text="To create a new vertical pane, you can press the Alt + Shift + + key combination. For a horizontal pane, you can use Alt + Shift + -" />
                    </TextBlock>
                </Grid>
            </Grid>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    ColumnSpacing="24"
                    Style="{StaticResource GridCardStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="560" />
                        <ColumnDefinition Width="*" MaxWidth="480" />
                    </Grid.ColumnDefinitions>
                    <controls:PictureHolder
                        Grid.Column="1"
                        Width="460"
                        Height="286"
                        Margin="-24"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        ImageUrl="ms-appx:///Assets/Picture2.png" />
                    <TextBlock TextWrapping="Wrap">
                        <Run
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="Profiles" /><LineBreak /><Run Text="Command Prompt, PowerShell, and bash (via WSL) are Profiles in Windows Terminal. You can customize these Profiles by going to Settings." />
                        <LineBreak />
                        <LineBreak />
                        <Run Text="From there, you can do things like set the profile to always run as Admin, set the starting directory, and change the profile name and icons!" />
                    </TextBlock>
                </Grid>
            </Grid>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    ColumnSpacing="24"
                    Style="{StaticResource GridCardStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="560" />
                        <ColumnDefinition Width="*" MaxWidth="480" />
                    </Grid.ColumnDefinitions>
                    <controls:PictureHolder
                        Width="460"
                        Height="286"
                        Margin="-24"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        ImageUrl="ms-appx:///Assets/Picture3.png" />
                    <TextBlock Grid.Column="1" TextWrapping="Wrap">
                        <Run
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="Keybinding Actions" /><LineBreak /><Run Text="Terminal is feature-rich, and every feature can be assigned a Keybinding Action. With Keybinding Actions, you can set custom keyboard shortcuts for small actions like Find to complex ones like Broadcast Input Mode." />
                        <LineBreak />
                        <LineBreak />
                        <Run Text="You can set keybindings by going to Settings &gt; Actions " />
                    </TextBlock>
                </Grid>
            </Grid>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    ColumnSpacing="24"
                    Style="{StaticResource GridCardStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="560" />
                        <ColumnDefinition Width="*" MaxWidth="480" />
                    </Grid.ColumnDefinitions>
                    <controls:PictureHolder
                        Grid.Column="1"
                        Width="460"
                        Height="286"
                        Margin="-24"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        ImageUrl="ms-appx:///Assets/Picture4.png" />
                    <TextBlock TextWrapping="Wrap">
                        <Run
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="SendInput Actions" /> <LineBreak /><Run Text="sendInput Actions are a special type of Action. These actions allow you to send input into the terminal, so you don’t have to remember lengthy commands" />
                        <LineBreak />
                        <LineBreak />
                        <Run Text="You can create new sendInput Actions by going to Settings &gt; Actions and selecting “sendInput” in the Action type. From there, you can give the Action a name and an Input." />
                        <LineBreak />
                        <LineBreak />
                        <Run Text="Every action (like the sendInput action) can be found in the Command Palette (Ctrl + Shift + P). Windows Terminal also has the Snippets Pane, a special UI that displays all your sendInput actions. Find the Snippets Pane by searching “Snippets” in the Command Palette or by using an assigned Keybinding Action. " />
                    </TextBlock>
                </Grid>
            </Grid>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    ColumnSpacing="24"
                    Style="{StaticResource GridCardStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="560" />
                        <ColumnDefinition Width="*" MaxWidth="480" />
                    </Grid.ColumnDefinitions>
                    <controls:PictureHolder
                        Width="460"
                        Height="286"
                        Margin="-24"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        ImageUrl="ms-appx:///Assets/Picture6.png" />
                    <TextBlock Grid.Column="1" TextWrapping="Wrap">
                        <Run
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="Rendering" /> <LineBreak /><Run Text="Windows Terminal has a powerful rendering engine that can support emojis, sixels, and shaders. You can even enable “Retro terminal effects” under your profile appearances. " />
                    </TextBlock>
                </Grid>
            </Grid>
        </FlipView>
    </Grid>
</Page>
